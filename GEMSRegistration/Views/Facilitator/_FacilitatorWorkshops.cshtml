@model List<Workshop>

@{
    ViewData["Title"] = "Facilitator Workshop Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h6 align="center">Workshop Details</h6>
<br />
@if (Model.Count > 0)
{
    <table class="table table-bordered table-condensed table-striped">
        <thead>
            <tr>
                <th>Workshop Name</th>
                <th>Participant First Name</th>
                <th>Participant Last Name</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var e in Model)
            {
                <tr>
                    <td>@e.WorkshopName</td>
                    <td>@e.Participant.FName</td>
                    <td>@e.Participant.LName</td>

                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div>No workshops to show at this time.</div>
}





